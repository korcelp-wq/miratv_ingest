📺 MiraTV Series Ingest Pipeline — Canonical State (Handoff)

Status: WORKING (with known open items)
Purpose: Reset context for new chat and continue development without re-debugging past ground
Design Philosophy: Accommodation & appeasement of hostile provider data

1️⃣ Core Principles (LOCKED)

RAW data is hostile

Never trust structure

Never assume valid JSON

Never block the pipeline

Routing is permissive

Grinding is strategy-specific

Unmappable ≠ failure (only quarantine for later analysis)

JSON is not a system structure — it is only a temporary island extracted from hostile text when useful.

2️⃣ Canonical Pipeline Flow (Current)
[0] Provider Fetch (Newman stdout)
        ↓
[1] RAW CAPTURE (verbatim text)
        ↓
[2.5] RAW ROUTER (TEXT-ONLY, NO JSON)
        ↓
 ┌───────────────┬───────────────┐
 │ default       │ arrays        │
 │ (legacy)      │ (Xtream maps) │
 └───────────────┴───────────────┘
        ↓                   ↓
[3] series_grinder.ps1   [3.5] series_grinder_arrays.ps1
        ↓                   ↓
[4] series_sep (canonical ingest files)