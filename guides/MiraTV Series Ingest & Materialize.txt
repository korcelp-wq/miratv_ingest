📺 MiraTV Series Ingest & Materialization
Authoritative State & Recovery Document

Status: ACTIVE / PARTIALLY WORKING
Last Known Good: STEP 8 (Raw Ingest)
Current Failure Point: STEP 9 (Materialization → HTML response)

🔒 PINNED NON-NEGOTIABLES (DO NOT BREAK)

These were explicitly called out and must be preserved.

1️⃣ No Local State Files

❌ No series_context.json

❌ No temp directories

❌ No local SQL

❌ No filesystem handoff between steps

All orchestration happens via HTTP → PHP → DB

2️⃣ Orchestration Model (LOCKED)
MASTER (batch)
  └─ triggers (PowerShell)
        └─ workers (PowerShell)
              └─ PHP (server-side)
                    └─ DB


Master never touches DB

Workers never touch DB

PHP does all DB writes

Triggers only orchestrate

Workers may contain light logic

3️⃣ Serial Pipeline (For Now)

One series at a time

Stateless

Restart-safe

Idempotent

Parallelism is deferred intentionally.

4️⃣ Flags Philosophy (Pinned Insight)

Flags should describe state, not block behavior

Queries decide what needs to be done

Flags are reconciled, not blindly trusted

details_ingested, raw_ingested, etc. are advisory

🧱 CURRENT PIPELINE (REALITY)
STEP 1–7

✅ Working

Series discovery

Pipeline locking

Raw fetch

Raw storage

STEP 8 — RAW TABLE PARSE

Trigger: 8raw_table_parse_trigger.ps1
PHP: raw_table_parse.php

Observed Output:

{"error":"no_embedded_payload","raw_id":1}


📌 Pinned Interpretation

Raw JSON exists

“payload” is not a literal field

JSON structure is valid Xtream-style (info, seasons, episodes)

STEP 8 is effectively complete enough to proceed

🔧 STEP 9 — MATERIALIZATION (DESIGNED BEHAVIOR)
Intent

Read RAW JSON from series_raw.raw_json

Extract:

info → series_details

seasons → series_seasons

episodes → series_episodes

Insert only if missing

Safe to re-run

No side effects

📂 SERVER-SIDE PHP (AUTHORITATIVE)
File:
/_ingest/series_materializer.php

Responsibilities

Authenticate via X-INGEST-TOKEN

Select next series where details_ingested = 0

Load raw JSON

Decode + extract domains

Populate prod tables

Return JSON only

📌 Important

Filename is series_materializer.php

NOT materialize_series.php

NOT UI-routed

Must bypass CSRF, sessions, login

🧑‍🏭 WORKER (CURRENT CORRECT VERSION)
File:
C:\miratv_ingest\workers\materialize_series_worker.ps1

Behavior

GET next series from:

/_workers/series_pipeline.php


Receives:

{ "series_id": 1 }


POST to:

/_ingest/series_materializer.php


Detects:

✅ JSON → success

❌ HTML → routing/auth failure (login, CSRF, UI)

Built-in Safety

HTML detection

Snippet printing for debugging

No temp files

No variable passing from master

🧨 CURRENT FAILURE (ROOT CAUSE)
Symptom

Worker successfully retrieves:

🎬 Processing series_id=1


POST returns HTML login page

Error includes:

Invalid CSRF
<!doctype html>

Diagnosis (Pinned)

This is NOT a PowerShell bug
This is NOT a variable passing issue
This is NOT a JSON issue

✅ The worker is functioning correctly
❌ The server is routing the ingest endpoint through the UI layer

🔴 WHY THIS IS HAPPENING

One (or more) of the following is true:

_ingest/ is not excluded from UI auth middleware

.htaccess rewrites POST requests to index.php

CSRF protection is globally applied

PHP file path mismatch (historically occurred)

Evidence:
The worker receives a full HTML login page, not JSON.

✅ WHAT IS WORKING (VERY IMPORTANT)

Series ID is successfully retrieved

Raw JSON exists in DB

Worker orchestration is correct

Pipeline logic is sound

Architectural breakthrough confirmed:

Stateless

Pull-based

Restart-safe

Independent raw → prod runs

This is not a design failure — it’s a routing fence.

📌 PINS FOR NEXT CHAT (CRITICAL)

When starting a new chat, include this list verbatim:

❌ No local files

❌ No local SQL

❌ No passing vars through master

✔ Worker pulls series_id itself

✔ PHP owns DB

✔ JSON-only ingest endpoints

✔ HTML response = server misrouting

✔ File is series_materializer.php

✔ Step 8 payload is implicit, not literal

▶️ NEXT IMMEDIATE ACTIONS (ORDERED)

Inspect .htaccess for _ingest/

Exclude _ingest/*.php from:

CSRF

session auth

UI rewrites

Ensure headers are read:

$_SERVER['HTTP_X_INGEST_TOKEN']


Confirm direct curl works:

curl -X POST https://miratv.club/_ingest/series_materializer.php \
  -H "X-INGEST-TOKEN: WYWIQ..." \
  -d '{"series_id":1}'


When curl returns JSON → STEP 9 will immediately go green.

🧠 META NOTE (KEPT INTENTIONALLY)

The “accidental breakthrough” is real

This pipeline now behaves like an instantiable execution unit

AI enrichment layer can be added cleanly as STEP 10+

This design reduces long-term complexity, not increases it